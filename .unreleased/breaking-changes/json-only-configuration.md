Configuration files now can be stored in `.apalache.json` or
`${user.home}/.tlaplus/apalache.json`, in strict JSON. HOCON `.cfg` files are
no longer supported. The JSON structure is simplified. Check the manual for
details. See PR #3409.
