Configuration files now use strict JSON and the supported `.apalache.json` /
`${user.home}/.tlaplus/apalache.json` names. HOCON `.cfg` files are no longer supported. The JSON structure is
simplified. Check the manual for details. PR #3409.
