Configuration files now use strict JSON and the canonical `.apalache.json` /
`$HOME/.tlaplus/apalache.json` names (migrate the old .cfg). HOCON is no longer
supported. The JSON structure is simplified. Check the manual for details. PR #3409.
