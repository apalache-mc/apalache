Replace the tuning keys `search.seed`, `search.simulation`,
`search.simulation.maxRun`, and `search.outputTraces` with the typed checker configuration fields `seed`, `search-kind`,
`max-run`, and `output-traces`, respectively, see #3404.
