package at.forsyte.apalache.tla.typecheck.passes

import at.forsyte.apalache.tla.imp.passes.PassWithOutputs

/**
 * The interface for the ETC type checker.
 */
trait EtcTypeCheckerPass extends PassWithOutputs
