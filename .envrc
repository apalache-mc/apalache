JAVA_LIB_DIR=$(pwd)/3rdparty/lib

if [[ "$OSTYPE" == "darwin"* ]]; then
    export JAVA_LIBRARY_PATH=$JAVA_LIBRARY_PATH:$JAVA_LIB_DIR
else
    export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$JAVA_LIB_DIR
fi

export PATH=$(pwd)/bin:$PATH
