\* Add statements after this line.
SPECIFICATION Spec
CONSTANTS 
    Producers = {p1,p2}
    Consumers = {c1,c2}
    BufCapacity = 1
\* generate dot file: add "-dump dot trans.dot" to TLC options
\* (=> extension settings)
INVARIANT Inv
