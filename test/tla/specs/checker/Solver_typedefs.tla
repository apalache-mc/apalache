------------------------- MODULE Solver_typedefs ------------------------------

(*
 @typeAlias: CONTEXT = {
      unary: Seq(Set(Set(Int))),
      binary: Seq(Set(Set(<<Int, Int>>)))
 };
 *)
Solver_aliases == TRUE

================================================================================
